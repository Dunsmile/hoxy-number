<svg width="1200" height="630" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- 배경 그라데이션 -->
    <linearGradient id="bgGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#1e3a8a"/>
      <stop offset="50%" style="stop-color:#7c3aed"/>
      <stop offset="100%" style="stop-color:#db2777"/>
    </linearGradient>

    <!-- 텍스트 그림자 -->
    <filter id="textShadow" x="-20%" y="-20%" width="140%" height="140%">
      <feDropShadow dx="0" dy="4" stdDeviation="8" flood-color="#000" flood-opacity="0.4"/>
    </filter>
  </defs>

  <!-- 배경 -->
  <rect width="1200" height="630" fill="url(#bgGradient)"/>

  <!-- 장식 원 (배경) -->
  <circle cx="200" cy="150" r="200" fill="white" opacity="0.05"/>
  <circle cx="1000" cy="500" r="250" fill="white" opacity="0.05"/>

  <!-- 메인 타이틀 - 정중앙 -->
  <text x="600" y="340" text-anchor="middle" font-family="Arial Black, sans-serif" font-size="120" font-weight="900" fill="white" filter="url(#textShadow)">Hoxy Number</text>

  <!-- 클로버 데코 -->
  <text x="600" y="450" text-anchor="middle" font-size="60">🍀</text>
</svg>
