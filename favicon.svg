<svg width="512" height="512" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <radialGradient id="ballGrad" cx="30%" cy="30%">
      <stop offset="0%" style="stop-color:#fde047"/>
      <stop offset="100%" style="stop-color:#f59e0b"/>
    </radialGradient>
    <filter id="shadow">
      <feDropShadow dx="0" dy="8" stdDeviation="12" flood-color="#000" flood-opacity="0.3"/>
    </filter>
  </defs>

  <!-- 배경 없음 (투명) -->

  <!-- 로또 공 -->
  <g filter="url(#shadow)">
    <circle cx="256" cy="220" r="160" fill="url(#ballGrad)"/>
    <text x="256" y="280" text-anchor="middle" font-family="Arial Black, sans-serif" font-size="200" font-weight="900" fill="white">7</text>
  </g>

  <!-- 클로버 -->
  <text x="256" y="480" text-anchor="middle" font-size="120">🍀</text>
</svg>
